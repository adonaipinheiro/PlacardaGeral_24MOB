<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.adonaipinheiro.placardageral">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentation.soccerGame.SoccerGameActivity"
            android:exported="true"
            android:theme="@style/ScreenTheme" />
        <activity
            android:name=".presentation.basketGame.BasketGameActivity"
            android:exported="true"
            android:theme="@style/ScreenTheme" />
        <activity
            android:name=".presentation.trucoGame.TrucoGameActivity"
            android:exported="true"
            android:theme="@style/ScreenTheme" />
        <activity
            android:name=".presentation.chooseGame.ChooseGameActivity"
            android:exported="true"
            android:theme="@style/ScreenTheme" />
        <activity
            android:name=".presentation.players.PlayersActivity"
            android:exported="true"
            android:theme="@style/ScreenTheme" />
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>